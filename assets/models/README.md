`disc.scad` contains an [openscad](https://www.openscad.org/) model.

After making changes to it, run the following to regenerate `disc.stl`:
```sh
openscad assets/models/disc.scad -o assets/models/disc.stl
```
